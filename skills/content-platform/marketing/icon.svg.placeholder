PLACEHOLDER — `icon.svg` for content-platform skill chip. SVG, viewBox `0 0 64 64`, 56×56 safe area, ≤8 KB. Suggest layered-pages mark or grid-of-blocks. Replace this file with the SVG; commit; send to OPVS support if already published.
